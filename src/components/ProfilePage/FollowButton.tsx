import { Button } from "@nextui-org/react";

const FollowButton = () => {
  return (
    <Button size="sm" variant="solid" className="font-semibold" color="primary">
      Follow
    </Button>
  );
};

export default FollowButton;
